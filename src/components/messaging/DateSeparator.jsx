export const DateSeparator = ({ label }) => (
  <div className="my-4 flex items-center justify-center">
    <span className="rounded-full bg-slate-100 px-3 py-1 text-[11px] font-medium uppercase tracking-[0.2em] text-slate-500 dark:bg-slate-800 dark:text-slate-400">
      {label}
    </span>
  </div>
);
